(Changed here in Original and pushed to be shared)
This repo has hello.java file that takes number as input and print the parity.
greet branch was created and greeting added to hello.java.
greet.java was built to get the git flow only(has no interfence with hello.java).
Worked with all tutorial labs on it.

